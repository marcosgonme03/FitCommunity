// DEPRECATED — reemplazado por lib/groq.ts (Groq con Llama 3.3, free tier).
// Este archivo se mantiene vacío para evitar romper imports antiguos durante la migración.
// Si nadie lo importa tras un build limpio, se puede borrar.
export {};
